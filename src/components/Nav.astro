
<nav>
    <div class="nav-logo"><a href="/">Logo</a></div>
    <ul>
        <li><a href="/">Home</a></li>
        <li><a href="">Download</a></li>
        <li><a href="/lista">Lista</a></li>
        <li><a href="">Sobre</a></li>
        <li><a href="">Login</a></li>
        <li></li>
    </ul>
    <button class="bg-purple-400 hover:bg-purple-600 text-white font-bold py-2 px-4 rounded-full">Cadastro</button>
</nav>

<style>

    nav {
        background-color: var(--branco);
        color: var(--roxo);
        padding: 1rem;
        display : flex;
        justify-content: space-between;

    }

     nav ul {
        list-style: none; 
        display: flex; 
        justify-content: flex-end;
    }

    nav li {
        float: right;
        margin: 0 20px;
        position: relative;
    }

    nav li::before{
        content: '';
        display: block;
        height: 3px;
        background-color: var(--lilas);

        position: absolute;
        bottom: -1rem;
        width: 0%;

        transition: all ease-in-out 200ms;
    }

    nav li:hover::before{
        width: 100%;
    }

    a {
        color: var(--branco); 
        text-decoration: none; 
        text-transform: uppercase;
    }

    a::before{
        
    }

    .nav-logo{
        font-size: 1.2rem;
        font-weight: bold;
        text-transform: uppercase;
        text-decoration: none;
        color: var(--lilas);
    }

    @media (max-width: 768px) { 
        nav {
            display: none;
        }
    }
</style>