<form action="post">
  <h1 class="tituloGradiente">Entre em Contato!</h1>
  <span>Como podemos ajudar?</span>
  <fieldset>
    <label for="name">Nome:</label>
    <input
      type="text"
      name="name"
      id="name"
      autocomplete="name"
      placeholder="Digite seu nome completo"
      required
    />
  </fieldset>
  <fieldset>
    <label for="email">Email: </label>
    <input
      type="email"
      name="email"
      id="email"
      autocomplete="email"
      placeholder="Digite seu email"
      required
    />
  </fieldset>
  <fieldset>
    <label for="description">Assunto: </label>
    <textarea
      name="description"
      id="description"
      cols="50"
      rows="10"
      placeholder="Descreva o problema para nós:"></textarea>
  </fieldset>
  <button
  class="button"
  type="submit"
    ><span class="button-content">Envie</span></button
  >
</form>

<style>
  form {
    border-radius: 15px;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    width: 40%;
    padding: 1rem;
  }

  span+fieldset {
    margin-top: 1.5rem ;
    font-size: 1rem;
  }

  fieldset {
    width: 100%;
    border: none;
    display: flex;
    flex-direction: column;
    margin-bottom: 1rem;
  }

  fieldset input,
  fieldset textarea {
    width: 100%;
    font-size: 1rem;
    margin: 0.3rem 0;
    padding: 0.5rem 1rem !important;    border-radius: 1rem;
    border: none;
    margin-bottom: 1rem;
    background-color: var(--secondary);
    resize: none;
  }

  fieldset input:focus, fieldset textarea:focus{
    outline: none;
    box-shadow: 0 0 0 1px var(--primary);
  }

  .button {
    background-color: var(--accent) !important;
    border-radius: 1rem !important;
    width: 100%;
  }

  .button:hover{
    cursor: pointer;
  }

  button{
    display: flex;
    justify-content: center;
    align-items: center !important;
  }

  span {
    font-weight: 500;
  }

  label {
    font-weight: bold;
  }

  @media only screen and (max-width: 768px) {
    form {
      width: 100%;
    }

    h1 {
      text-align: center;
    }
  }
</style>
